# QA / Definition of Done (Chelse)

## Constants

- toast ≠ commit  
- Confirm before write (Cancel = zero rows)  
- Section isolation (no cross-section writes)  
- History retained; prior snapshots queryable  
- Conflict → discrepancy; never auto-merge  
- Companion to GCSS — PDF/inject never invents APSR accountability alone  
- Photo = visual layer only

## Sprint 2 — 2062 in

- Electronic **and** OCR paths both hit Confirm  
- Bad/partial parse never silent-commits  
- Wrong section/UIC → deny, no rows  
- Success asserts DB + history (or disabled if D1 down)

## Later slices

- Out: return date required; ≤30d / past due = warn only  
- 1750 = packing layer only  
- CHR language = COEI / BII / AAL (not BOM)
